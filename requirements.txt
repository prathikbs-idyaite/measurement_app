streamlit>=1.36
streamlit-webrtc>=0.47
opencv-python-headless>=4.9
numpy>=1.26,<2.1
pandas>=2.0
rembg>=2.0.56
onnxruntime>=1.17
pillow>=10.0
scipy>=1.11

# SAM 2 segmentation (much better than rembg for body masks)
# First run downloads the SAM 2 tiny checkpoint (~40 MB)
sam2>=1.0

# MediaPipe: 0.10.15+ dropped the legacy `mp.solutions` API. measure_engine.py
# supports BOTH the new Tasks API and the legacy one, so any of these work.
mediapipe>=0.10.14

# SMPL-X engine (optional — enables ±1-1.5cm accuracy)
torch>=2.0
smplx>=0.1.28
trimesh>=4.0